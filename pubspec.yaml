name: dice_tower
description: A flutter dice rolling package.
version: 0.5.1
homepage: https://github.com/martin-labanic/dice_tower

environment:
  sdk: ">=2.1.0 <3.0.0"
  flutter: ">=1.12.13"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  effective_dart: ^1.2.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
#  plugin:
#    platforms:
#      android:
#        package:
#        pluginClass:
#      ios:
#        pluginClass: